DECLARE
CURSOR C1 IS SELECT EMPLOYEE_ID, SALARY FROM EMPLOYEES
WHERE DEPARTMENT_ID =80
FOR UPDATE;
BEGIN
FOR I IN C1
LOOP
EXIT WHEN C1%NOTFOUND;
UPDATE EMPLOYEES
SET SALARY = SALARY + SALARY * 0.2
WHERE CURRENT OF C1;
DBMS_OUTPUT.PUT_LINE(I. EMPLOYEE_ID II'...'||1. SALARY);
END LOOP;
END;